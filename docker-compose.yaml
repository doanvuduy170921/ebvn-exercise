version: '3.8'

services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - 6379:6379

  bookmark-service:
    image: go-api
    build: .
    container_name: bookmark_service
    depends_on:
      - redis
    ports:
      - 8080:8080
    environment:
      - REDIS_PASSWORD=doanvuduy123
      - REDIS_ADDR=redis:6379
